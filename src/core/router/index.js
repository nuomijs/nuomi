const listeners = [];

export const listener = () => {};

export const removeListener = () => {};

export const createRouter = () => {};

export const matchPath = () => {};

export default {
  listener,
  removeListener,
  matchPath,
};
